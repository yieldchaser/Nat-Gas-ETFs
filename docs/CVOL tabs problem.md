Problem: The CVOL tab is substandard compared to our other tabs. Here’s what’s wrong and what needs to happen.
1. Tooltips are completely missing.
Look at the Trough to Peak tab — every single KPI card, every chart element, every metric has visible, detailed tooltips. That tab is the gold standard. Our CVOL tab has zero tooltips anywhere. Fix this across every element.
2. We built far too little with the data we have.
All we have right now is:
 ∙ A chart
 ∙ Some KPI cards at the top
 ∙ 5 mini-charts (SAD, CI, CDC, MDS, etc.) that are essentially useless — you can’t hover them, can’t expand them, can’t view any detail
 ∙ A signal event timeline
That’s it. We have extremely valuable CVOL data — NGVL, NGDN, NGUP, NGSK, ATM, Convexity, Underlying — and this is all we could extract from it? That’s unacceptable.
3. The 5 bottom mini-charts are broken.
 ∙ No x-axis labels, no y-axis labels
 ∙ You can’t hover for values
 ∙ You can’t expand them to see full history
 ∙ They’re just random lines on a blank canvas — completely unreadable and useless in their current state
4. KPI cards are incomplete.
Missing key metrics. Compare to Trough to Peak — every card there tells you something actionable. Ours don’t.
What I need — a comprehensive improvement plan:
 ∙ Audit every element against the Trough to Peak tab as the quality benchmark
 ∙ Add tooltips to literally everything — cards, charts, timeline events, mini-charts
 ∙ Make the mini-charts functional: axis labels, hover data, expandable to full view
 ∙ Identify what additional metrics, charts, and signals we should be extracting from this data but aren’t
 ∙ The CVOL data is a goldmine — if we’re not using it fully, that’s on us. The plan should maximize every column we have.
Reference: Use the Trough to Peak tab as the design standard for everything & other tabs for other standards like the Volume Monitor tab is also pretty fucking complex ( complex is automatically not equal to good but still). Some good things worth adapting might also be there in the Flow Monitor tab